@model About

@{
    ViewData["Title"] = "Hakkımızda Güncelleme";
}

<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit" enctype="multipart/form-data">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <input type="hidden" asp-for="Image" />
            <input type="hidden" asp-for="CreatedDate" />
            <div class="form-group">
                <label asp-for="Description" class="control-label"></label>
                <textarea asp-for="Description" class="form-control" cols="50" rows="30"></textarea>
                @* @Html.TextAreaFor(model => model.Description, new { @id = "editor" }) *@
                <span asp-validation-for="Description" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Image" class="control-label"></label>
                <input asp-for="Image" class="form-control mb-1" type="file" />
                <span asp-validation-for="Image" class="text-danger"></span>
                @if (Model.Image != null && !string.IsNullOrWhiteSpace(Model.Image))
                {
                    <img src="/img/about/@Html.DisplayFor(modelItem => Model.Image)" height="50" />
                    <label class="form-check-label">
                        @Html.CheckBox("cbResmiSil") Resmi Sil
                    </label>
                }
            </div>
            <div class="form-group mt-2">
                <input type="submit" value="Kaydet" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Geri Dön</a>
</div>

<script src="~/ckeditor/ckeditor/ckeditor.js"></script>
<script>
    CKEDITOR.replace("editor");
</script>
